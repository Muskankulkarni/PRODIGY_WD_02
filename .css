body {
  font-family: Arial, sans-serif;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background-color: #B777D7;
}

.stopwatch {
  text-align: center;
}

.display {
  font-size: 3em;
  margin-bottom: 20px;
}

.controls button {
  padding: 10px 20px;
  margin: 0 5px;
  font-size: 1em;
  cursor: pointer;
  border: none;
  border-radius: 5px;
}

.controls button:hover {
  background-color: #ddd;
}
